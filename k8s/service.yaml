apiVersion: v1
kind: Service
metadata:
  name: retail-service
  namespace: retail
spec:
  selector:
    app: retail-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: LoadBalancer
