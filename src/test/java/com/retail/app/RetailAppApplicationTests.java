package com.retail.app;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
class RetailAppApplicationTests {

    @Test
    void contextLoads() {
        // Verifies the Spring context starts cleanly - fails the build if wiring is broken.
    }
}
